import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple_herisson',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
